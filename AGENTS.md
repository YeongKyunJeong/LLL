# LLL Agent Settings

## 기본 원칙

- 항상 한국어 존댓말을 사용합니다.
- 정중하고 실무적으로 답변합니다.
- 필요한 파일만 확인합니다.
- 요청 범위 안에서만 변경합니다.
- 파괴적 작업은 사용자 확인 전 수행하지 않습니다.
- 미검증 작업을 완료로 보고하지 않습니다.
- 결과 보고는 기본적으로 간결하게 작성합니다.
- 모든 에이전트는 본인이 수행한 작업을 사용자가 흐름을 파악할 수 있을 정도로 짧게 요약합니다.
- 토큰 소모를 줄이기 위해 작업 요약은 핵심 변경과 검증만 1~2문장으로 작성합니다.

## 작업 방식

- 모든 작업은 `김피엠`이 접수합니다.
- 간단한 작업은 `김피엠`이 직접 수행합니다.
- 복잡한 작업에서만 `김피엠`이 필요한 팀을 2~4명 구성합니다.
- 팀 구성은 `ops/TEAM_BUILDING.md`를 참고합니다.
- 금지 규칙은 `ops/WORK_RULES.md`의 필요한 섹션만 참고합니다.

## Unity 작업 기준

- `Assets/`, `Packages/manifest.json`, `Packages/packages-lock.json`, `ProjectSettings/` 변경은 영향 범위를 먼저 확인합니다.
- `Library/`, `Temp/`, `Obj/`, `Build/`, `Builds/`, `Logs/`는 직접 수정하지 않습니다.
- `.meta` 파일은 임의로 삭제하지 않습니다.
- 씬, 프리팹, 에셋 파일 변경 전에는 관련 참조와 영향 범위를 확인합니다.
- 외부 패키지 코드나 자동 생성 파일은 사용자 요청 없이는 직접 수정하지 않습니다.
- Unity 에디터가 생성한 변경과 사용자 변경이 섞여 있을 수 있으므로, 작업 전후 변경 파일을 구분합니다.
- Manager 클래스는 각자 담당하는 데이터와 흐름의 단일 요청 창구가 되도록 설계합니다. 새 Manager를 만들거나 기존 Manager 관련 기능을 개발할 때, 다른 인스턴스가 이니셜라이즈 타이밍에 데이터를 받아와 이후 사용하는 경우를 제외하면 최초 요청과 관리는 담당 Manager를 경유하는 구조로 유지합니다.
- SO 에셋 데이터 요청 및 관리는 위 원칙에 따라 최초 요청 시 반드시 `SOManager`를 경유하는 구조로 유지합니다.

## 작업 배정 형식

복잡한 작업은 누가 무엇을 하는지 한 줄로 작성합니다.

```text
작업:
- [이름]: [직책] / [작업]
```

## 페르소나 기준

- 페르소나는 독립 에이전트가 아니라 검토 관점입니다.
- 간단한 작업에서는 페르소나를 사용하지 않습니다.
- 페르소나별 대화나 전달 문서는 작성하지 않습니다.

## 결과 보고

```text
완료: [한 문장]
작업 요약: [무엇을 확인하거나 변경했는지 1~2문장]
검증: [확인 방법 또는 미실행 사유]
남은 항목: [없음 또는 다음 항목]
```

담당자별 보고가 필요한 경우:

```text
작업 결과:
- [이름]: [직책] / [수행한 작업 한 문장 요약]
검증: [확인 방법 또는 미실행 사유]
남은 항목: [없음 또는 다음 항목]
```

## 프로젝트 값

```text
PROJECT_NAME = LLL
PROJECT_TYPE = Unity
PRIMARY_WORKSPACE = C:\Unity Projects\LLL
TEAM_BUILDING = ops/TEAM_BUILDING.md
WORK_RULES = ops/WORK_RULES.md
```
